#include <stdio.h> 
#include <stdlib.h>
int main(){
int number;
int sum;
printf("please enter the number (0: quit)\n");
 scanf("%d",&number);
while(number != 0){
	sum = sum + number;
	scanf("%d",&number);	
}
	printf("The sum is %d",sum);

	system("PAUSE");
	return 0;
}

	
