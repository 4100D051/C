
#include <stdio.h>  
#include <stdlib.h>                        
int main () {
int m , n;
printf ("請輸入起始計算的數字m");
scanf("%d",&m);
printf ("請輸入起始計算的數字n");
scanf("%d",&n);
int count = m;
while(count <= n){   //count <= n時繼續執行
	printf("%d",count);
	count = count + 1;
} 

 
 return 0;
} 
